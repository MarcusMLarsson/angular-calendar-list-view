import { IsSelectedDayPipe } from './is-selected-day.pipe';

describe('IsSelectedDayPipe', () => {
  it('create an instance', () => {
    const pipe = new IsSelectedDayPipe();
    expect(pipe).toBeTruthy();
  });
});
